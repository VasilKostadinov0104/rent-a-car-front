/**
 * this is the current width of the device that is considered `mobile`
 */
export const mobileWidth = 768
/**
 * this is the current width of the device that is considered `tablet`
 *
 */
export const tabletWidth = 768

/**
 * this is the current width of the device that is considered `desktop`
 */
export const desktopWidth = 1024
